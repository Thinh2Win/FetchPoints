const request = require('supertest');
const async = require('async');
const { payers } = require('../../data.js');
const app = require('../../index.js');

describe('post route test', () => {
  test('POST /transactions', (done) => {
    
  });
});